<h1><%= @album.title %></h1>

<% @images.each do |image| %>
  <div>
    <h2><%= image.title %></h2>
    <p><%= image.description %></p>
    <p><%= image.url %></p>
  </div>
<% end %>

<%= link_to 'Back to Profile', user_path %>
