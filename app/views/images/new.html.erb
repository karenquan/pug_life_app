<div class="add container">
  <h1>Add Image to <%= @album.title %> Album</h1>
  <%= form_for [@album, @image], html: { multipart: true } do |f| %>
    <%= f.label :url, 'Image URL' %>
    <%= f.file_field :url %>

    <%= f.label :title %>
    <%= f.text_field :title %>

    <%= f.label :description %>
    <%= f.text_area :description %>

    <%= f.submit 'Submit' %>
  <% end %>
  <%= link_to 'Back to Album', album_path(@album), class: "back" %>
</div>


